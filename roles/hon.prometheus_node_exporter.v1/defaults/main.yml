# You may want to override this to pin it
prom_node_exp_version: '1.9.1'
# Add the daemon user to certain groups,
# e.g. if you have enabled hidepid and the user needs to be part of the configured group for full system visibility
prom_node_exp_user_extra_groups: []
# Args to pass to the binary, typically to configure collectors
prom_node_exp_args: []
# Textfile collectors to add and accompanying cron jobs
prom_node_exp_textfile_collectors_custom: []
# prom_node_exp_textfile_collectors_custom:
#   xxx:
#     bin_src: 'xxx.sh'
#     cron_job: '0 * * * * root /opt/prometheus/node-exporter/textfile-collectors/xxx | sponge /var/lib/prometheus/node-exporter/textfile-collectors/xxx.prom'
prom_node_exp_textfile_collectors_enabled: []
# prom_node_exp_textfile_collectors_enabled:
#   - apt
#   - zfs
#   - zpool
