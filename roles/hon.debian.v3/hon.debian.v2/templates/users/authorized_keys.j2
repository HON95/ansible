#jinja2: trim_blocks:False

{% for key_name, key_value in ssh_keys.items() %}
{{ key_value.options + " " if key_value.options is defined and key_value.options else "" }}{{ key_value.type }} {{ key_value.key }} {{ key_name }}
{% endfor %}
