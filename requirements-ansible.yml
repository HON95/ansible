# Ansible dependencies provided in a single file for convenience.

collections:
  - name: ansible.netcommon
  - name: ansible.posix
  - name: community.general
  - name: community.docker
  - name: containers.podman
  # FIXME
  # - name: lae.proxmox
  - name: vyos.vyos
