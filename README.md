# Ansible Stuff

Collection of my Ansible roles and stuff.

## Dependencies

Run `requirements/install.sh` or see the `requirements` directory.

### Git Repositories

Can be added as submodules to control versioning through tags.

- `https://github.com/lae/ansible-role-proxmox`
