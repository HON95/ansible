# Post-login banner (/etc/motd) (define to enable)
#linux_server_banner_motd_path: motd.txt
# Enable pretty MOTD with dynamic system info (requires extra scripts and packages)
linux_server_banner_dmotd_enable: false
